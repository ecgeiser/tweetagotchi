<%= form_for(@pet) do |f| %>
	Name: <%= f.text_field :name %><br>
	Species: 
	<% @type.each do |animal| %>
		<%= f.radio_button :pet_type_id, animal %>
		<%= f.label :pet_type_id, animal.capitalize, value: animal %>
	<% end %>
	<br><%= f.submit %>
<% end %>