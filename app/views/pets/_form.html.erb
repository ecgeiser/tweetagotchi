<%= form_for(@pet) do |f| %>
	Name: <%= f.text_field :name %><br>
	Species: 
	<% @types.each do |pet_type| %>
		<%= f.radio_button :pet_type_id, pet_type.id %>
		<%= f.label :pet_type_id, pet_type.animal.capitalize %>
	<% end %>
	<br><%= f.submit %>
<% end %>