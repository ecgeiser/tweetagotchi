<%= form_for(@pet) do |f| %>
	Name: <%= f.text_field :name %><br>
	Species: 
	<% @types.each do |species| %>
		<%= f.radio_button :pet_type_id, species.id %>
		<%= f.label :pet_type_id, species.animal.capitalize %>
	<% end %>
	<br><%= f.submit %>
<% end %>